#include "databaseManager/dbManager.h"
#include <stdio.h>
/*
DatabaseManager::DatabaseManager() {
	SAConnection con;
	try {
		con.Connect("Experiments", "user", "password", SA_MySQL_Client);
		printf("Connected\n");
	}
	catch (SAException &x) {
		con.Rollback();
		printf("%s\n", (const char*)x.ErrText());
	}
}*/